# S20.5 managed desktop enrollment — repository ownership transfer

Status: implementation and validation in progress; no release or merge claim.

## Scope

The user requested that desktop changes accompanying the Kubernetes lifecycle
land in `tunnexio/tunnex-client`, not reintroduce the retired desktop modules in
`tunnexio/tunnex`. This transfers existing committed source, not a new recovery
protocol or an expansion of the Kubernetes walk.

- Source: `tunnexio/tunnex` PR #56, content
  `33661ca056102f0f6991603d224408ad538f5fde`.
- Destination baseline: `tunnex-client` main `4d42485`.
- Preserve the standalone package/release version, current tray assets and
  behavior, and the standalone macOS scoped-resolver permissions fix.
- Transfer the managed-enrollment shell, organization selection, durable local
  enrollment state, authenticated request binding, renderer/IPC contract and
  associated tests together. Do not copy server-only generated API changes.
- No helper source changes are present in the committed S20.5 source delta.
  Incomplete uncommitted helper/Windows recovery work stays outside this PR.
- Windows automatic terminal crash recovery remains deferred to S20.5.1;
  this extraction does not claim to implement it.
- Keep the existing installed application, profiles, credentials, tunnel,
  cloud infrastructure and control plane untouched during source validation.

## Validation boundary

Run client and renderer typechecks, automated tests and builds after transfer.
Verify ownership and preserve all destination-only fixes. Hosted platform checks
and an installed-build compatibility walk remain separate from local tests;
do not describe a source transfer as a freshly packaged live-client proof.
The client and control-plane PRs must cross-reference their version dependency.

## Local transfer verification — 2026-09-06

- All 35 transferred source/test files matched the monorepo merge-base in the
  destination before transfer; no destination-specific source fix was overwritten.
  The package manifest transfers only the test command, preserving version 0.1.1.
- Client typecheck/build and renderer typecheck/build passed.
- Client suite: 293 passed. Changed renderer suite: 64 passed.
- Broader renderer suite: 80 files and 1,103 tests passed; two unchanged suites
  fail to load missing standalone fixtures (`openapi/openapi.yaml` and
  `docs/design/TUNNEX-wireframe-v2.html.txt`). Those tests and missing paths are
  unchanged from destination main. This is not a full-suite green result.
- Root `pnpm typecheck` also encounters the existing shared-package script with
  no tsconfig. Direct client and renderer commands above pass. This PR does not
  silently alter unrelated baseline test wiring or claim that command passes.
- Helper, tray behavior/assets and dependency lockfile remain byte-unchanged.
- CI additionally runs the changed renderer suite so the IPC/UI side of this
  transfer is tested on both desktop platforms, not only compiled for packaging.
- No new installed-client/live recovery test was performed for this transfer.
