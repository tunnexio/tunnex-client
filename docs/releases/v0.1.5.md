# Tunnex Desktop v0.1.5

Includes merged PR #8: relay preparation clamps its deadline to the earlier of
CP expiry and the helper local bound, correcting the reproduced clock-tolerance
mismatch without extending authorization. Known helper failures now surface
safe reasons instead of only relay_helper_prepare_failed; raw secrets/errors
are not exposed.

This does not establish the exact cause of the reported customer Mac failure.
The native customer relay walk remains pending after installation. Split-tunnel
relay prerequisites and existing signing configuration are unchanged. Direct-first
orchestration is not introduced by this release.

User authorized release when CI is green. Both manifests are0.1.5; publish only
through the existing tag workflow after exact-head preparation checks pass.
