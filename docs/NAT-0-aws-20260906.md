# AWS NAT-0 live ledger

Status: native AWS TCP and TLS traffic passed. User approved AWS continuation.
Existing CP/database untouched. Infrastructure created for this proof only:

- Account `735391218823`, region `ap-south-1`.
- Instance `i-0d320abd28be9eaa9`, name `tunnex-nat0-aws-20260906a`, t3.small.
- VPC `vpc-00a41471ec7bce5d2`, subnet `subnet-00578ce6ebdedf28b`.
- Security group `sg-0bfd623ee6d39467d`: laptop /32 only, TCP 22/13478/15349;
  no UDP ingress. Existing CP security group was not modified.
- AMI `ami-0c0fd09cfe77b59dc` (Ubuntu noble amd64, verified available).
- Encrypted gp3 root volume, 12 GiB, retained with the instance.
- Kernel fixture image ID on AWS:
  `sha256:675191759e5ba1ec0014d06e63269909c6c740b0b1a7d25bc50b9502309fa0af`.
  Pinned coturn base is the same 4.17.2-r0 index as the native proof.

## Review

Two reviewers identified non-atomic SCP signaling publication. The required
correctness fix was folded under the user's continued authorization: transfer
to temporary filenames, then rename on the receiving side. Both closed it.
Ownership remains mode0600 with the private directory's owner; an initially
suspected ownership issue was withdrawn after checking the committed code.

## Initial attempts

- `tunnex-aws-pion-20260906T135059-48737`: TURN allocations and SSH signaling
  succeeded, ICE connection timed out. No successful cloud packet proof. Mac
  helper restarted idle. Fixture stopped and retained.
- `tunnex-aws-pion-20260906T135520-49414`: unprivileged ICE-only diagnostic also
  timed out; it did not stop the helper or create host routes. Coturn granted
  peer permissions and showed bidirectional peer packets.
- `tunnex-aws-pion-20260906T135732-49605`: fixed-category diagnostics reported
  maximum ICE requests reached. Testing a readiness barrier so both sides have
  exchanged candidates before starting connectivity checks; local shared-file
  success did not exercise SSH signaling latency.

Private keys, TURN/ICE credentials, certificates and raw signaling are outside
Git. Relay remains a synthetic fixture, not CP-managed signaling or policy.
Stop the dedicated instance at the end; do not delete it or touch CP/database.

## Readiness barrier and live results

The runner now atomically releases a start marker only after both peers publish
prepared markers after exchanging candidates. This avoids starting connectivity
checks while SSH is still delivering the other peer's signaling. The successful
retry supports the signaling-timing diagnosis; no ICE authentication or TLS
verification was weakened.

- `tunnex-aws-pion-20260906T140302-49995`: unprivileged TCP ICE-only diagnostic
  passed on both roles (7.76s / 10.94s). Not traffic evidence.
- `tunnex-aws-pion-20260906T140347-50131`: native macOS backend to AWS Linux
  kernel WireGuard over TURN/TCP passed encrypted HTTP, two reachable positive
  controls, cryptokey deny with unchanged application count, allowed liveness,
  and relay-close failure. Linux counted exactly three application arrivals.
  Helper readiness passed. Fixture stopped and retained.

Focused tagged race tests passed; these compile but skip live proof without
explicit fixture variables. Review found P2 (HELD): ICE-only diagnostic mode
hardcodes plaintext client TURN even if passed `tls`. Only TCP diagnostics were
used; normal native TLS follows its separate mode-aware root runner. Do not
claim diagnostic TLS qualification. No CP policy/GUI acceptance is implied.

- `tunnex-aws-pion-20260906T140455-50527`: native TLS repeated all TCP traffic
  assertions successfully; Linux counted exactly three application arrivals.
  Separate unprivileged process rejected the untrusted certificate with exact
  x509 unknown-authority classification and zero candidates. Fixture stopped
  and retained. Both independent reviewers found no further barrier issues;
  the diagnostic-only TLS P2 remains held for disposition.

Post-walk: development helper is idle, no fixture 10/8 routes remain, default
route is en0, and `/etc/pf.conf` matches the preserved baseline byte-for-byte.
Tagged race tests, vet and shell syntax checks passed. No remote exact-SHA CI,
CP-policy enforcement, GUI relay lifecycle, or automatic direct-to-relay
fallback qualification is claimed. Dedicated EC2 stop requested; retained EBS
storage remains billable. Existing CP/database were not mutated.

## Approved diagnostic correction and next boundary

User approved the held P2. ICE-only mode now selects the client URL from the
requested TCP/TLS mode. Two offline regression cases execute the actual runner
configuration with a mocked AWS identity/host; both passed, including independent
reviewer execution. The reviewer closed P2 without new findings. CI runs this
small regression on macOS; it never accesses AWS. The normal native traffic path
is unchanged, so the successful cloud packet runs were not repeated. Diagnostic
TLS itself is not newly claimed as live-tested.

Dedicated instance state was subsequently verified **stopped**. Volume and
private artifacts are retained; EBS storage is still billable.

Next narrow integration boundary: replace fixture-only signaling with the
NAT-1 authenticated session contract, preserving the internal helper bind and
kernel per-peer bridge. Before production implementation, reconcile remaining
NAT-0 acceptance explicitly: the cloud run proves packet transport, not CP-issued
resource policy or an automatic failed-direct-to-relay transition. Do not rebuild
the cloud or rerun unchanged TCP/TLS packet cases to stand in for these missing
capabilities. Contract/design work can identify those dependencies without
claiming NAT-0 complete or enabling relay in existing installations.

## Mixed-candidate feasibility — NOT GO yet

User requested feasibility before production implementation. NAT-1 is paused.
The dedicated instance was restarted, retaining its verified SSH host key under
the original host-key alias (public IP changed after stop/start).

Only the dedicated SG was changed: temporary rule `sgr-04afbde5e4761a999`
allowed UDP15000 from the verified laptop /32 for a direct positive control.
That exact rule was then revoked successfully; readback showed TCP22/13478/15349
only and no UDP ingress. Existing CP/database and their security groups were
not modified. The fixture publishes UDP15000 so the SG, not missing Docker port
mapping, determines direct reachability.

- `tunnex-aws-pion-20260906T142021-52504`: with UDP allowed and host+relay
  candidates, Pion selected host/host on the client, host/prflx on the server.
  Native encrypted HTTP and all existing request controls passed; three server
  arrivals. The separate certificate-negative subprocess initially inherited
  mixed mode and failed its zero-candidate assertion because host candidates
  existed. This was a harness assertion failure, not a traffic pass withdrawal.
- `tunnex-aws-pion-20260906T142120-52770`: after UDP rule removal, with both
  host and relay candidates still offered, ICE timed out on both peers at 90s.
  Fixed-category logs reported maximum requests reached. No tunnel/HTTP pass.
  Selected-path fallback is therefore NOT proven. This is a fresh-negotiation
  test, not an established-session migration or reconnect test.
- Certificate-negative correction: explicitly clear expected-path mode only
  for the separate negative subprocess. Live rerun against the second fixture
  passed exact unknown-authority plus zero candidates in 0.14s. No TLS bypass.

Mixed mode uses upstream Pion nomination, not a custom fallback state machine.
Both host and relay candidate types were confirmed in exchanged signals. The
loopback TURN allocation topology worked for relay-only; its suitability for
mixed candidate nomination remains an unproven hypothesis, not a diagnosed
upstream defect. Next diagnostic should inspect pair-state/response counters
or reduce this topology, not build production signaling around the failure.

Independent focused review reported no actionable code findings and explicitly
limited the claim to fresh negotiation. Tagged race/vet and offline transport
selection regressions passed. Both fixtures are stopped/retained; dedicated EC2
stop requested. Helper is idle, fixture routes absent and PF baseline unchanged.

Policy feasibility remains unexecuted, not green. Source mapping identified
`policy.Compile` / `Service.CompiledForNode` and node `egress.New`, `SetPolicy`,
`Reconcile`. A real test must route to a behind-gateway service through FORWARD;
gateway-local INPUT listeners and cryptokey denial cannot substitute. A synthetic
compiler snapshot would prove compiler/renderer/kernel compatibility only, not
DB-backed CP issuance/authenticated delivery. No final feasibility GO is claimed.

## Topology reduction: blocked-direct selection and traffic PASS

- `tunnex-aws-pion-20260906T142759-53760`: reproduced the mixed-candidate
  timeout without privileged helper execution. Post-failure pair statistics
  were empty; no counter-based root-cause claim is made.
- Removed the fixture-only `relay-ip=127.0.0.1` and `allow-loopback-peers`
  configuration. Coturn now chooses its normal interface address. No Pion
  patch, nomination override, forced relay selection or authentication change.
- `tunnex-aws-pion-20260906T143008-54077`: mixed ICE with the corrected topology
  passed unprivileged (9.34s client / 12.78s server); client selected relay/prflx,
  server host/relay. This alone was not packet evidence.
- `tunnex-aws-pion-20260906T143101-54192`: same mixed mode passed actual macOS
  backend to AWS kernel encrypted HTTP, reachable denial control, cryptokey
  refusal, allowed-path liveness and closed-transport failure. Three server
  arrivals. Separate exact unknown-authority/zero-candidate TLS negative passed.

The dedicated SG still has no UDP ingress; mixed candidates were offered and
Pion chose relay-assisted connectivity during fresh negotiation. Direct success
under the temporary /32 UDP allowance was recorded above. This supports a
fixture-topology cause of the previous failure, not an upstream defect claim.
It does NOT qualify mid-session path migration, reconnect, Windows, CP-issued
application policy or GUI behavior. Independent focused review found no
actionable topology correction findings. Production NAT-1 remains paused.

Post-run authenticated CP inventory (read-only after login) confirmed the BYODB
Neon organization has zero enrolled nodes and zero devices, zero-trust mode off.
License reports Community/unlicensed; this is recorded as inventory, NOT assumed
to prove that policy is unavailable (the current one-binary policy service exists).
No enrollment, policy, licence or database mutations were made. A real enrolled
gateway plus behind-gateway service is still needed for CP-issued policy proof.

Post-run SG readback showed TCP only, helper reported down, and PF baseline
matched. Dedicated EC2 stop requested; retained disk still incurs storage cost.
Feasibility status: fresh-negotiation fallback PASS; CP-issued policy PENDING.
